VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
# Chave pública VAPID (Web Push) — gera com `npx web-push generate-vapid-keys`.
# A chave PRIVADA nunca entra aqui nem no código: fica só como secret da Edge Function.
VITE_VAPID_PUBLIC_KEY=
